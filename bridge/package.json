{
  "name": "bun-dap-bridge",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun build src/main.ts --target=node --outdir=../../bin --outfile=bridge.js && echo '#!/usr/bin/env node' | cat - ../../bin/bridge.js > ../../bin/bridge && chmod +x ../../bin/bridge && rm ../../bin/bridge.js"
  },
  "dependencies": {
    "ws": "^8.20.1",
    "semver": "^7.8.1",
    "source-map-js": "^1.0.2"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "latest",
    "@types/ws": "latest"
  }
}
